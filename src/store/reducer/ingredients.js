const initialState = {
    salad:0,
    cheese:0,
    bacon:0,    
    meat:0
};

const ingredientsReducer = (state = initialState, action) =>{
    return ( 
        state 
    );
}

export default ingredientsReducer;